import { describe, it, expect } from 'vitest'

describe('Sum', () => {
    it('adds 1 + 2 to equal 3', () => {
        // This is a placeholder test to verify the runner.
        // We will make it fail first to ensure we are seeing "RED".
        expect(1 + 2).toBe(3)
    })
})
